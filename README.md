# EdgeGuard
EdgeGuard is an ESP32-CAM based edge AI security node that performs lightweight on-device human presence / intrusion detection, triggers a local alert, and publishes events to an IoT backend over Wi-Fi.
